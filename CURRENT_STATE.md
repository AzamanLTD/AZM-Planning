# AZAMAN Current Engineering State

**Last reconciled:** 2026-09-17 UTC
**Authority:** current repository `main` + exact CI/evidence.

## Current verified baseline

- Backend main: `6802ab6` (2026-09-15) — invoice creation/payment concurrency proofs, prior financial/dine-in hardening, the route-checker mount-registry fix (`6b7a884`), the merged P0 adapter authority proof (PR #236), the admin dine-in lifecycle projection endpoint (`117cebe`), PR #241 control-plane integrity: every privileged control-plane mutation (permission replacement, department create/update, staff create, suspend/activate/deactivate, staff PATCH, duty assign/revoke) now commits atomically with its STAFF audit event in one transaction; authority-sensitive UPDATEs are TOCTOU-guarded (validated-state WHERE clause, zero rows -> 409); concurrent permission replacement is serialized by FOR UPDATE on the target profile after the new race test proved parallel replacements could commit a union of both grant sets. Exact-head CI green at `f5aabaa` (1149 tests), post-merge main-head run green at `6d8da48`; prod healthy. Schema note: control-plane tables exist only in migration `20260828040000_control_plane_staff_access` (not schema.prisma models) — `prisma db push` cannot provision them; the integrity suite applies that migration's DDL idempotently for prod-schema parity.

- **Financial-integrity wave (2026-09-14, verified):** PR #243 (squash `fdcd596`) made escrow dispute raise/resolve atomic with canonical transaction-scoped financial primitives; PR #244 (squash `66b39b1`) closed the business no-show settlement defect — processBusinessNoShow now runs refund (canonical `_refundEscrowTx`), guarded stake penalty claim, conditional booking terminal transition and the canonical AuditLog row in ONE transaction with no swallowed failures, idempotent replay, and escrow-linked ownership verification; bookingEscrowService release/refund/split-release bucket drains are guarded conditional claims; schema + additive migration add `BusinessProfile.stakeBalance` (the column the penalty policy always assumed but never existed — the pre-fix code could never have applied a penalty). Real-PostgreSQL proofs run in CI (penalty-policy-integrity: 10 DB cases; escrow-dispute-integrity; control-plane integrity). Exact-head CI green at `8c6e24e` (1181 tests); post-merge main-head run green at `66b39b1`.
- **Financial-integrity wave, deposit + P2P settlement batch (2026-09-14, verified):** PR #245 (squash `0b6cf6a`) guarded concurrent invoice wallet debits; PR #246 (squash `ad88db3`) made fiat deposit settlement exactly-once and hardened the P2P escrow settlements: (1) `localFiatDepositWebhook` + `moolreCollectionWebhook` — the PENDING→COMPLETED conditional `updateMany` is the database-enforced winner claim, executed BEFORE the wallet credit in the SAME transaction; concurrent duplicate SUCCESS webhooks can no longer double-credit, losers converge without mutation, credit failures roll the claim back to PENDING (retryable), replays are side-effect free; Tatum left unchanged (unique txHash insert inside the settlement transaction already guarantees rollback-safe exactly-once). (2) `completeTrade` — the escrow direction was INVERTED vs the authoritative `tradeController` accept-flow (SELL ad → vendor pool locked; BUY ad → trade.userId locked): the old code debited the NON-escrow-holding party and paid the fiat payer twice; now both branches debit the correct bucket via a guarded `gte` claim and pay the fiat payer the net, and the vendor margin cut is actually paid on both paths (conservation: net + vendorCut + adminCut === amountCrypto). (3) `markUnderpaid` — the full escrowed principal now leaves the escrow owner bucket in one guarded claim (the old code credited the paid portion without debiting the escrow: unbacked credit + funds stuck in escrow). (4) `flagOverpayment` — split drains are guarded conditional claims. **REAL DEFECT the new DB tests exposed:** `TransactionHistory.type = 'OVERPAYMENT_FREEZE'` (the Phase H11 idempotency lock) was never added to the `TransactionType` enum — on real Postgres the insert failed the enum constraint and the whole flag transaction rolled back, so EVERY buyer overpayment flag was broken in production; fixed with an additive enum value + idempotent migration `20260914150000_overpayment_freeze_enum`. New suites on real PostgreSQL: `deposit-settlement-integrity` (8 cases), `p2p-settlement-integrity` (13 cases); `seedPaidTrade` fixture + trade-flow/admin-force-release assertions corrected to the authoritative escrow direction. Exact-head CI green at `d40c299` (1205 tests); post-merge main-head run green at `ad88db3`. Backend main is now `ad88db3`.

- **Financial-integrity wave, deposit-address authority batch (2026-09-17, verified):** PR #275 (exact head `4ce3de4`, squash `ea75f05`) closes the deposit-address authority gap. `User.tatumPolygonAddress` was a mutable legacy string with no registry, lifecycle, or uniqueness; deposit owner resolution matched against it. The canonical `WalletAddress` registry now guarantees at the DB level: `(address, network)` globally unique, exactly one ACTIVE row per user+asset identity (partial unique index), RETIRED addresses never reassigned; allocation is race-convergent; `resolveOwner` is registry-first with the deterministic legacy-mirror fallback; the canonical deposit identity is native Polygon USDC (`0x3c499c542cef5e3811e1192ce70d8cc03d5c3359`), with bridged USDC.e explicitly rejected. Idempotent migration `20260917150000` + boot overlay; legacy column kept and synced (never removed). New real-PostgreSQL suite `wallet-address-governance` (13 cases incl. 8-way allocation race convergence and DB-enforced cross-owner uniqueness). CI at `501134e` exposed 9 real-PG failures, all reproduced on a local Postgres 15 and root-caused before fix commit `4ce3de4` (nonexistent `User.updatedAt` in backfill SQL, adoption collision with RETIRED mirror rows, 43-char test fixture vs VARCHAR(42), per-test user deletion violating TransactionHistory FK). Full 1402-test suite verified 1402/1402 green locally before push; exact-head CI green at `4ce3de4`; backend main is now `ea75f05`. Details: `progress/2026-09-17_wallet-address-authority.md`.

- **Financial-integrity wave, §P.1 closure-hardening (2026-09-17, verified):** PR #276 (exact head `e7cac0f`, squash `639784d`) closes the post-merge audit gaps in PR #275: (1) `body.userId` is no longer a financial ownership authority — the Tatum crypto webhook resolves ownership ONLY through the WalletAddress registry from the actual deposit address; a payload userId can never choose the credited user. (2) `resolveOwner` grants ownership ONLY for an ACTIVE canonical (POLYGON / USDC / native-contract) row — RETIRED addresses resolve to no active owner, and the registry state always wins over the legacy mirror (a retired address cannot be resurrected via `User.tatumPolygonAddress`); history/audit preserved via separate `lookupWalletAddressHistory`. (3) The corrupted USDC_E constant was corrected to the official Polygon USDC.e contract `0x2791bca1f2de4661ed88a30c99a7a9449aa84174`, with exact-contract tests pinning both identities. (4) Explicit `USDC.E` webhook deposits are rejected before any financial mutation — native Polygon USDC is the only canonical customer deposit identity. Proofs: wallet-address-governance extended to 18 real-PG cases (userId-override regression, unknown-address no-credit, USDC.E zero-mutation, RETIRED/mirror resurrection blocked), suite made hermetic after the webhook singleton credits leaked into withdrawal-fee-discount-atomicity; full backend suite 1407/1407 green, prisma validate clean, route-check PASS, npm audit 0 vulns. Backend main was `639784d` at that reconciliation. §P.2 subsequently landed 2026-09-17 (see the §P.2 wave below). Details: `progress/2026-09-17_wallet-address-authority-closure.md`.
- **Financial-integrity wave, §P.2 KMS-capable custody execution (2026-09-17, verified):** PR #277 (exact head `11d3799`, exact-head CI green run #1028, squash `994f07c`) lands the external custody execution boundary. Explicitly distinguished states: KMS-capable implementation landed TRUE; Tatum provider contract implemented TRUE; KMS/four-eye validation boundary implemented TRUE; **production live signing/broadcast ACTIVE = FALSE**; **real testnet execution exercised = FALSE** (CI is deterministic mock-provider proof); **§P.3 custody accounting = NOT STARTED**. `services/tatumCustodyExecutionService.js` is the single canonical external crypto execution boundary (sweeps execute through it too); native Polygon USDC only (USDC.e rejected); exact base-unit internal arithmetic with provider-facing decimal conversion (`baseUnitsToDecimalString`, `Prisma.Decimal` authoritative writes — no JS float money); durable `CustodyExecution` with `tatumPendingId` from the real `SignatureId` response (txHash only ever from real evidence — no fabricated hashes); atomic single-winner CAS submission claim (a second external submission is structurally impossible; crash-after-claim stays ambiguous); asynchronous lifecycle `REQUESTED→RESERVING→SUBMITTED→SIGNING→BROADCAST→CONFIRMING→COMPLETED` with real-evidence reconciliation, terminal states never moving backward; unknown outcomes stay `RECONCILIATION_REQUIRED` with no automatic refund (only definitive `PROVIDER_REJECTED` refunds, atomically, exactly once); `COMPLETED` requires independently verified chain-transfer evidence (branded proof — a forged object cannot settle); the current ERC-20 token endpoint `POST /v3/blockchain/token/transaction` (`ChainTransferEthErc20KMS`: chain MATIC, decimal-string amount, digits 6, `signatureId`, `index` only for mnemonic-based IDs) with the documented KMS pending endpoints (the fictional approve endpoint removed); documented four-eye externalUrl validation contract (`GET /api/internal/custody/kms/validate/:pendingId`, 2xx only for a durably APPROVED, exact-matching, non-terminal execution with a registry-verified signer; mandatory on MAINNET); no raw private-key execution path anywhere; customer deposit signer and master-hot signer are distinct identities verified against the ops-populated KMS signer registry (`tatum-kms getaddress` proofs, fail-closed in LIVE mode); `WalletAddress` remains the §P.1 deposit-address authority; production gates remain explicit and OFF unless separately authorized. The PR also carries the pre-merge amendment (head `11d3799`) that fixed 14 contract-faithfulness blockers; custody proofs grew 57→72 on real PostgreSQL with zero regressions (the 11 failing full-suite suites are pre-existing localhost:5432 environment suites, identical on clean main). No production financial data changed. Post-merge main-head CI green at `994f07c` (run 105304499068, verified against GitHub). Backend main is now `994f07c`.

- **Financial-integrity wave, §P.4 preparation (2026-09-18, verified investigation):** Backend main is `be6038f0528b9e7c62a203e99708f16198a5d898`. Live code review confirms the existing `JournalEntry` is still a shadow journal: `services/journalService.js` owns a separate PrismaClient/transaction, uses JS floating-point parsing/epsilon validation, and several financial paths invoke it post-commit or fire-and-forget. The current journal helpers also model customer `available` as an asset instead of the target customer-liability semantics, and major financial paths still rely on User balance buckets and legacy singleton bookkeeping. §P.4 therefore transitions the existing journal foundation (no parallel ledger) into a caller-transaction-scoped authoritative liability ledger, keeps User bucket columns as materialized projections, establishes restricted-obligation semantics, connects ledger postings to P3 custody truth, and adds deterministic reconciliation. No production financial backfill or balance correction is authorized. Canonical investigation: `progress/2026-09-18_P4_LIABILITY_LEDGER_INVESTIGATION.md`; Planning commit `afca3b6`.

- **Financial-integrity wave, §P.3 custody accounting (2026-09-18, verified):** PR #278 (final exact head `8e2a070a`, squash merge `be6038f0528b9e7c62a203e99708f16198a5d898`) is merged. The custody layer now has durable `CustodyAccount`, `CustodyMovement`, and `CustodyEvidence` structures; §P.1 `WalletAddress` remains the deposit-address authority; §P.2 `CustodyExecution` remains the external execution state machine. Deposit webhooks are candidate observations only and must pass independent Tatum v4 transaction evidence before a movement is VERIFIED. Account-balance evidence is structurally separated from transaction evidence and only fresh accepted balance evidence can feed the PoR reserve numerator. Custody quantities use exact BigInt base units / exact Prisma.Decimal persistence. The PoR liability model is denomination-honest: X includes completed crypto and fiat-settled-USDC external flows, Y is the evidence-linked subset, and inconsistent X > mixed-pool states fail closed as `LIABILITY_FLOW_EXCEEDS_MIXED_POOL`; unsupported/unclassified exposure is never silently removed. Legacy `SystemMasterCrypto`/`SystemHotWallet` balances contribute ZERO authoritative reserve assets. Restricted obligations remain explicitly unmodeled, so §P.3 cannot claim a fully-backed state until that denominator is authoritative. Webhook exact-quantity parsing and custody identity resolution now occur before financial mutation; malformed/over-precision/scientific amounts and non-registry custody identities produce zero financial mutation. No historical production balances were corrected or backfilled by P3. Verification evidence: two consecutive full backend batteries 221 suites / 1549 tests each green; route-check PASS; backup/restore drill SUCCESS; exact-head CI run `35312694766` green on `8e2a070a`; PR #278 squash merge `be6038f...` verified on GitHub. Production live crypto signing/broadcast remains OFF; real testnet execution remains NOT exercised. §P.4 is next: restricted-obligation/custody economics authority and the liability-ledger transition, followed by the planned inventory/route/liquidity work. §P.3 (CustodyAccount / CustodyMovement / custody asset-liability accounting / evidence-backed Proof of Reserves / treasury location semantics) is deliberately NOT started. Details: `progress/2026-09-17_p2-kms-custody-execution.md`.
- **Financial-integrity wave, §P.4 authoritative liability ledger (2026-09-18, verified):** PR #279 merged to backend main as squash `f3f72a652d2d361729214a48239a3cf08cf217ea` from final head `06d379cba8c2ee3c4e1f5c264550cba92d0c50d3`. P4 establishes the caller-transaction-scoped authoritative double-entry liability ledger with exact Decimal validation, durable idempotent posting identities and fail-closed balancing. All major customer financial writer families are now ledger-posted in the same transactions as their materialized User/domain projections: deposits, fiat quote deposits, peer transfers, withdrawals, SmartEscrow/booking escrow, dispute settlement, vendor pool allocation, vault/savings, Susu, order-book settlement, business invoices, chat transfers, administrative adjustments and P2P/trade settlement/reversal paths. Restricted-obligation coverage is complete: pending external fiat/crypto withdrawals are represented as persisted `RestrictedObligation` rows and count separately in the reserve denominator; internal escrow/dispute/vendor-unallocated reclassifications remain inside customer liability and are not double-counted. Crypto deposit webhooks first create provisional `clearing:custody:unverified:usdc` liability-backed clearing; independent chain evidence is required to reclassify into authoritative `custody:deposit:usdc`; definitive mismatches move to explicit rejected suspense. Fiat off-ramp SUCCESS uses `clearing:fiat:offramp:usdc`, never fake provider-held USDC custody, until the later GHS-liquidity wave models actual fiat asset movements. PoR now has one effective denominator authority: current customer liability projections + modeled external restricted obligations. TransactionHistory flow classification is reconciliation evidence only. `totalLiabilities`, reserve ratio, coverage, `isFullyBacked`, attestation and invariant payload derive from that same denominator. Custody reconciliation distinguishes VERIFIED/CANDIDATE/FAILED movements and reports provisional/rejected clearing states explicitly. Final CI evidence: workflow #1041 / run `35374960201` SUCCESS on the exact premerge head, including full 223-suite / 1,593-test battery, route registry verification, production dependency audit, disposable-Postgres tuning and backup/restore drill. Post-merge main is `f3f72a65`; no production financial data was repaired or rewritten and live crypto signing/broadcast remains OFF. Details: PR #279 and final P4 audit comment `5733901703`.

- **Backend hygiene + RBAC authority wave (2026-09-15, verified):** PR #247 (squash `a6b025a`) serialized shift-scheduling mutations under transaction-scoped advisory locks and recorded the void-deserialization platform finding; PR #248 (merge `6be5399`) executed that follow-up — the tax preset, order tracking and storefront draft/publish advisory-lock calls all use the deserializable `SELECT 1 AS locked FROM pg_advisory_xact_lock(hashtext($1))` shape via `$queryRawUnsafe`, proven by 13 new live-PostgreSQL advisory-lock-integrity cases (exact-head CI run #949 green at `773f705`). PR #249 (merge `dc701bb`) closed the PR #240-flagged RBAC gap: `COMPLIANCE_ADMIN` now holds `withdrawals.approve` (catalog extension — the tier system remains the sole amount-scoping authority; amount-conditional action permissions were rejected as a second authorization dimension), making the >= $10k tier declarations and the >= $50k Finance-or-Compliance participation invariant genuinely executable by Compliance; 5 new mocked regressions + a real-Postgres $60k Compliance/Finance concurrent CAS proof (exact-head CI run #951 green at `f2c836c`; post-merge main-head run #952 at `dc701bb`). Both PRs passed route-check, prisma validate, production dependency audit and the db backup/restore/recovery drill; full-suite failures are only the 5 pre-existing localhost:5432 environment suites, identical on clean main. Backend main is now `dc701bb`. Since then three more PRs merged 2026-09-15: PR #250 (squash `13d7884`) closed the P0 fee-discount atomicity gap — the AZM fee-discount debit now runs on the canonical withdrawal transaction (dedup fee_discount_<ref>), reversal restores the spend once (PENDING->FAILED one-winner), azm_spend emits exactly once post-commit, proven by the real-Postgres withdrawal-fee-discount-atomicity suite (exact-head green at `8b1ac9c`, 200 suites/1236 tests); PR #251 (squash `f0f2f1c`) made dashboard invoice stats authoritative from aggregates with a regression test; PR #253 (squash `977f116`, issue #252) closed two P0s — force-cancel (trade claim + escrow refund + message + FORCE_CANCEL_TRADE audit row in ONE transaction via strict audit mode, deterministic 409 loser for concurrent cancels) and MTN payout unknown-outcome classification (unknown outcomes stay PROCESSING with a reconciliation exception; settle/replay idempotent; idempotent TEXT-key installer for the raw-SQL reconciliation tables; 203 suites/1260 tests green on real PostgreSQL). Backend main is now `977f116`.
- **AZM ledger idempotency + atomic auction settlement wave (2026-09-15, verified):** PR #254 (squash `4efd269`) closed the P0 idempotency defect — concurrent identical `creditAzm()`/`debitAzm()` calls could both mutate `User.azmBalance` because dedup was a read-then-write JSON-metadata pre-check with no database-level uniqueness. Dedicated nullable `dedupKey` column + `@@unique([userId, source, dedupKey])` on `AzmRewardLog` and `AzmSpendLog` — the DB unique index is the actual concurrency gate; a racing duplicate log insert fails the index and the whole enclosing transaction (balance mutation included) rolls back. Additive history-preserving migration with duplicate-audit NOTICE and first-occurrence-wins backfill (no ledger row deleted/rewritten), proven on real PostgreSQL incl. idempotent re-runs and the template-copy backup/restore drill. Real-PostgreSQL `azm-idempotency-concurrency` suite (8 cases, regression-proven); exact-head CI green at `4fe4597`, post-merge main-head run green at `4efd269` (204 suites / 1268 tests). PR #255 (squash `6802ab6`) then closed the P0 auction settlement defect — `settle()` burned winners via independent `debitAzm()` calls with swallowed errors, then unconditionally marked bids WON, boosted ads and SETTLED the auction, so a winner without funds still got the full burn-free boost (ledger/auction state permanently disagreed). Settlement is now ONE authoritative transaction: atomic OPEN→SETTLING conditional claim, winner debits through the transaction-scoped `_debitAzmWithClient` primitive with deterministic `auction-win-${auctionId}-${vendorId}` dedup keys (exactly-once via #254's unique invariant), WON/boost/LOST/SETTLED together, `totalAzmBurned` from actual committed debits, notifications/emission strictly post-commit. Fail-closed: a failed winner debit rolls the whole settlement back and the 5-minute worker retries; a committed SETTLING is impossible under this design, and legacy stranded SETTLING rows are recovered burn-free via the widened claim + dedup keys. No settlement policy existed in docs/specs, so per the fail-closed default no new economics were invented — the existing worker is the recovery mechanism. Real-PostgreSQL `azm-auction-settlement-integrity` suite (9 cases; 4 provably fail on the old implementation); exact-head CI run green at `cf3b01e`, post-merge main-head run `34994019112` green at `6802ab6` (205 suites / 1277 tests). Route-check, prisma validate, production audit and recovery drill green on both PRs. Backend main is now `6802ab6`.
- **Audited finding (2026-09-15, OPEN — needs product decision before a fix slice):** `azmStakeService.createStake` never debits `User.azmBalance` and never checks it — Nitro stake tier (which gates premium storefront widgets via `PREMIUM_WIDGETS`, incl. `custom_html` at NITRO_GOLD) is freely inflatable with unbacked stakes; `completeUnstake` returns nothing because nothing was ever taken. No doc/spec/test specifies stake fund movement (the stake-policy contract test asserts tier math only), so this is a product-contract gap, not a provable code regression: do NOT unilaterally invent debit-on-stake/credit-on-unstake semantics — record and decide first. The adjacent AZM spend surfaces (ad boost, fee discount) already share the hardened `_debitAzmWithClient` primitive and showed no new defect.
- Flutter main: `c750562d26499346e7c43315fba9912951e590d1` — durable dine-in recovery plus the new payment convergence contract are merged and verified.
- Business Portal main: `0d8a58d` (2026-09-15) — PR #88 pointer palette insertion, Wave A token/wiring slices, PR #93 bounded device-frame scrolling, PR #96 Wave A completion, PR #98 dine-in lifecycle invalidation proof, PR #97 (executable dine-in realtime projection runtime test), PR #99 (genuinely scrollable studio phone frame) and PR #100 (squash `b9b5481`, Wave C rendered evidence: component-owned device-frame overflow contract, gallery horizontal-scroll preservation, viewport-button responsive relayout with a byte-identical persisted-document proof — the earlier tautological non-mutation assertion was hardened on the same branch at `50cf9ca`, CI run #286 green). Wave C then closed at the browser level: #102 (`928c19a`), #103 (`73cd9ce`), #104 squash-merged as `0d8a58d` with exact-head and post-merge main-head CI green — see the Wave C section below. Main-head validate green.
- Business Portal Wave C browser-evidence wave (2026-09-15): **COMPLETE.** PR #101 was closed/superseded at 824 changed lines against the <=500 contract (independent review catch); split 1:1 into three stacked PRs, all now MERGED. **#102** merged as squash `928c19a` (production rendering fix: unit-suffixed CSS shorthands in StorefrontPhonePreview, bare device frame in editor mode; 6 new vitest regressions, 198 total; post-merge main-head validate green). **#103** merged as squash `73cd9ce` (Playwright scroll half A+B/C/D + harness + config, 471 lines, single commit rebuilt from main `928c19a` as head `7dcc1e0`; post-merge main-head push CI green at `73cd9ce`). **#104** was then rebuilt once more from the post-#103 main (head `6885980`, single commit, base main `73cd9ce`, diff exactly 162 additions / 0 deletions over two files: E responsive relayout + F token geometry + Chromium/Playwright CI wiring), mergeable clean; exact-head workflow_dispatch CI green at `6885980` (run 34981353993: smoke, vitest, Playwright Chromium E2E, build — all success); squash-merged by Sugru as `0d8a58d` with the exact head SHA pinned, and post-merge main-head push CI green at `0d8a58d` (run 34987297733). Wave C closes with genuine end-to-end rendered evidence at both the DOM and browser level; the earlier 'rebuild #104 again from resulting main' requirement is satisfied and superseded by this merged state. Original #101 remains closed/superseded.
## Studio acceptance audit — current truth

Historical Wave A/B/C completion is not trusted merely because an older green run exists. Each acceptance list is checked against current code and matching executable evidence before completion is recorded.

| Wave | Status | Code-verified evidence | Still required |
|---|---|---|---|
| A — Token foundation | **VERIFIED / complete** | PR #96 exact head `fb20134bdd0ae622c8937e29c01f5e11a33a0abf` merged at `2ef57f22b1dcae45334dd5f30e896e448073f10b`; exact CI passed smoke, tests, and production build. Current preview consumes grounded shared tokens across all 16 catalog renderers plus action/fallback/selection/chrome/frame geometry. The executable completion guard verifies all 16 renderers and rejects numeric inline CSS `px` literals. | None under the current Flutter measurements. Reopen only if the source geometry contract changes. |
| B — Pointer drag with magnetic snap | **REOPENED / partial — 2D prerequisite decision made** | PR #88 merged Pointer Events palette insertion with capture, pointermove/up/cancel cleanup, before/after hit testing and click suppression. Current `StorefrontStudioV2.jsx` mounts the semantic layer tree + phone-preview stage, not the legacy 2D `StorefrontCanvas.jsx` magnetic-snap surface. The legacy canvas itself still contains a coherent shared-token drag/snap implementation, but it is not mounted by V2. | Keep palette insertion as the current V2 drag acceptance. **Do not reintroduce the historical 2D canvas solely to satisfy old criteria.** Magnetic snap/fuse/settle stays deferred until a deliberate V2 2D surface defines coordinate ownership, persistence semantics, collision/geometry contracts, and executable tests. |
| C — Real device emulation | **REOPENED / partial** | PR #93 merged a bounded `studio-device-scroll-viewport` with vertical auto-scroll, horizontal clipping and overscroll containment around the transformed device preview. Device dimensions/scaling are tokenized. | Exercise rendered end-to-end scroll with genuinely overflowing content; prove phone/tablet/desktop responsive relayout and clipping/overflow behavior against rendered UI. |

## Active Studio implementation

- PR #87 is **closed/superseded**, not merged. Exact-head fix commit `e62b6c7ae99ca8771ad3ee1e177d4321f3c1ec73`; exact-head run #252 passed, but the PR exceeded the normal change budget.
- PR #88 merged at `76e39eb6ba937082684cc72189c34f7157b967a8`; exact-head run #253 passed smoke/tests/build.
- PR #89 merged at `59d9567d72b4eb0798a1a97f2fc9877381725a26`; exact head `cc37a2058b8c68ba1ddcbe6663f86e1474af110d3`, run #255 passed.
- Historical Business Portal PR #91 is the second token-map slice; it merged at `a1002e0846f7160298dc58e3f8261e675a9a1c5f`; exact head `df1bcd0d2f6903d396bc95affec9311b306fbfa9`, run #258 passed.
- PR #92 merged at `fc4536492c2c879ad2ff2b47aefe5c6c83dcc226`; exact head `08955de01a48bcbcac3e621c7741e2781a3d4843`, run #261 passed. It wires Showcase Gallery, Location Map and Video Player to grounded tokens.
- PR #93 merged at `83c1749e887f20846ec7af4876e5291816115ec7`; exact head `0952e426dfcf451ebfecd51c8ea3ee9afa2d8a16`, run #262 passed. It adds the bounded device-frame scroll boundary.
- PR #94 merged at `f5e8470898b87462d9febb152e0d8faafef4569e`; exact-head CI passed. It grounded/wired Promo Banner, Social Feed and Live Stats.
- PR #95 merged at `c64a2f78e68781402beb7a89ec90340097ac9503`; exact-head CI passed. It added the remaining Animated Counter, Custom HTML and Gradient Hero token maps.
- PR #96 merged at `2ef57f22b1dcae45334dd5f30e896e448073f10b`; exact head `fb20134bdd0ae622c8937e29c01f5e11a33a0abf`, exact CI passed smoke/tests/build. This closes current Wave A implementation and acceptance.
- PR #98 merged at `7140658f4d66cada3d6c3155c085638105fe484e`; exact head `85560129a19470e40a70175a8049eb3cffde8655`, Business Portal CI run `33975428983` passed smoke/tests/build. The test executes all supported `DINE_IN_TAB_*` lifecycle events and verifies canonical dine-in query-root invalidation.

## Dine-in P0 — current evidence and residual proof

- Backend is authoritative for `OPEN → FINALIZED → CLOSED`; the canonical invoice service remains the financial authority.
- Backend PR #232 merged at `7afbe1a7318402519aafed6142fdb7364f9df86d` with exact-head `186ac370bc75c60d5980a0bb8c3d308f812b94f0` and production audit/tests/database recovery passing; recovered paid settlement also emits the business-owner lifecycle notification.
- Backend PR #233 merged at `526f659f83af5d7fc708a0de964abad707d5a6a7` with exact-head `2453729846cf15d95bf6ab637d26ae19f3b735fa`, run `33957019469`, and tests/production audit/database recovery passing. It proves FINALIZED → invoice → PAID → CLOSED with tip and deterministic `DINE_IN_TAB:<tabId>` idempotency, plus durable PAID/CLOSED replay without a second payment mutation.
- Backend PR #234 merged at `205bfae0082303253638c40828df9928f820d7cb` with exact-head `97709b8d5d42494c460997dd0053eabfc7a2dea6`, proving concurrent payment claim/replay safety.
- Backend PR #235 merged at `ad6110213f5a859fd9e47db75d0f36682c32974e` with exact-head `d18e486fb1bd3bd07f2c3973113e9a422c9c0a1d`, proving concurrent invoice creation converges on the same idempotent invoice.
- Flutter PR #90 merged at `bf0589583522b44965a63379486ab33cb9d484e2` with exact CI `e479e07546d723afb48d6d841ebfecde452b30f8`, proving the durable CLOSED-tab recovery parser.
- Flutter PR #91 merged at `c750562d26499346e7c43315fba9912951e590d1`; exact head `d1dbc94ed890583241a4d338fc2a045cf5cec4a3`, Flutter Quality run `33967426867` passed Analyze/Test with coverage. The added contract verifies payment POST → authoritative tab reread → durable CLOSED recovery, while preserving the original failure when durable proof is absent.
- Business Portal PR #98 merged at `7140658f4d66cada3d6c3155c085638105fe484e`; exact head `85560129a19470e40a70175a8049eb3cffde8655`, run `33975428983` passed. Coverage executes all supported `DINE_IN_TAB_OPENED`, `DINE_IN_TAB_ITEM_ADDED`, `DINE_IN_TAB_ITEM_REMOVED`, `DINE_IN_TAB_FINALIZED`, `DINE_IN_TAB_PAID`, and `DINE_IN_TAB_CANCELLED` notification paths and proves unrelated order events do not invalidate dine-in projections.
- **P0 scope now verified at cross-client contract level:** backend settlement/replay authority, customer durable recovery, and business-owner notification→canonical-query convergence each have current executable evidence. Socket payloads remain invalidation signals rather than payment proof.
- **Admin-side dine-in lifecycle projection (2026-09-14, verified):** Backend `117cebe` adds `GET /api/admin/dine-in/overview` (protect+adminOnly; full zero-defaulted OPEN/FINALIZED/CLOSED/CANCELLED lifecycle counts, closed-today count, 24h closed volume+tips in USDC, 15 recent tabs with business+invoice joins; jest contract tests 3/3). Admin Portal `730cb18` consumes it on the Dashboard (API + `useDineInOverview` hook + lifecycle panel + vitest contract test; local vitest 13/13, tsc/build/eslint clean). This closes the "no dedicated dine-in projection" finding: exact-head CI is green on backend `94dfd31` and adminPortal `730cb18`.
- **Residual integration gap:** there is still no single deployed four-surface E2E harness tying the live Backend, Flutter, Business Portal and Admin surfaces together through one physical finalize/payment/replay/reconnect scenario. With the Admin projection in place, that harness now has an Admin surface to read.

## Important contracts

- Dine-in financial authority remains backend-owned and uses deterministic durable idempotency/replay rather than duplicate money movement.
- Studio remains semantic-tree based with no AI generation, prompt compiler, free-form canvas, or localStorage persistence.
- Current Wave B decision is to avoid legacy 2D restoration solely for historical snap criteria.
- Planning completion claims require current-code evidence plus matching executable tests/CI; historical green runs alone never close a wave.

## Residual risks / hygiene

- Wave C evidence: COMPLETE 2026-09-15 — JSDOM-rendered slice merged at `b9b5481`; browser-level fix merged (#102, squash `928c19a`); scroll half merged (#103, squash `73cd9ce`); responsive/token half merged (#104, head `6885980` rebuilt from post-#103 main, exact-head dispatch CI green, squash-merged as `0d8a58d` with post-merge main-head CI green at `0d8a58d`). Business Portal main is now `0d8a58d`.
- A live deployed four-surface dine-in E2E harness remains future integration work; the current P0 claim is explicitly contract-level, not deployed-E2E. The Admin-side lifecycle projection is now implemented and pending exact-head CI confirmation.
- Hygiene findings (2026-09-14 audit): (1) `scripts/route-checker.js` had been broken by the src/routes mount-registry refactor — fixed at `6b7a884`, and it is not wired into backend CI, which is why nothing caught it; consider adding it as a CI step. (2) CLOSED 2026-09-15 — adminPortal PR #98 (merge `60faed7`) wired the four vitest lib suites into CI conventionally (vitest 4.1.11 devDependency, `npm test`, CI `npm run test`), replacing PR #97's ad-hoc undeclared `npx vitest` step; 13/13 green. (3) The production backend on Render cold-starts in ~27s (free-tier spin-down) — a production-readiness concern for the release wave. (4) 190+ stray backend branches (including `noop`, `temp`, `x`, `accidental-do-not-use`) need a cleanup sweep.
- Bundle-size warning (>500 kB chunk) remains a separate performance slice.
- Backend branch-deletion cleanup remains optional hygiene only where safe tooling supports it.
